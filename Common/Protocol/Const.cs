using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

public class Const
{
    public const int CLIENT_TEMP_RETINUE_ID_NORMAL = -1; //默认值，普通召唤的随从
    public const int CLIENT_TEMP_RETINUE_ID_SUMMON_PREVIEW_NOT_CONFIRM = -2; //预召唤随从上场但未选择目标


    public const int TARGET_RETINUE_SELECT_NONE = -2; //无指定目标的随从
    public const int CARD_INSTANCE_ID_NONE = -1; //无对应卡牌实例（凭空生成的牌，不参与卡组循环）
}